# Backend deps for the portfolio FastAPI service.
# Trimmed to only what server.py actually imports so Render's free-tier
# build stays fast (the original template included pandas/numpy/boto3
# which the portfolio doesn't use).

fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv>=1.0.1
pydantic>=2.6.4

# MongoDB (optional — see backend/.env.example). Required because
# server.py imports motor, but the actual Mongo connection only opens
# if MONGO_URL + DB_NAME are set.
motor==3.3.1
pymongo==4.5.0

# Used by the /api/behance/{username} server-side scrape
requests>=2.31.0
